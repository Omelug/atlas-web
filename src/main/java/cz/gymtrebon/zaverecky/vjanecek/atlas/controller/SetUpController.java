package cz.gymtrebon.zaverecky.vjanecek.atlas.controller;

/*
@Controller
@RequiredArgsConstructor
@RequestMapping("/admin")
public class SetUpController {
    private final AppSettingsRepository appSettingsRepository;

    @GetMapping("/setup")
    public String showSetupForm(Model model) {
        model.addAttribute("settings", new AppSettings());
        return "setup";
    }

    @PostMapping("/setup")
    public String handleSetupForm(@ModelAttribute AppSettings settings) {
        appSettingsRepository.save(settings);
        return "redirect:/admin"; // Redirect to the main admin page
    }

    // Add other controller methods for managing the app
}*/
